# xueqiu-monitor Phase 1 dependencies
#
# Runtime
numpy>=1.24.0
scikit-learn>=1.3.0
anthropic>=0.49.0          # LLM sentiment analysis (optional — degraded to keyword if missing)
playwright>=1.58.0         # Crawler (installed via xueqiu-analyzer; listed here for isolated envs)

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Development
mypy>=1.5.0
