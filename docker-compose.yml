services:
  xueqiu-monitor:
    build: .
    container_name: xueqiu-monitor
    restart: unless-stopped
    volumes:
      - ./etc:/app/etc:ro
      - ./data:/app/data
    environment:
      - FEISHU_WEBHOOK_URL=
